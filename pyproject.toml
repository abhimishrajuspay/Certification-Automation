[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cz-certification-automation"
version = "0.12.1"
description = "Cited CZ portal evidence, LLM synthesis, and Postman generation"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "playwright>=1.40,<2.0",
    "pydantic>=2.0,<3.0",
]

[project.scripts]
cz-crawl = "scraper.cli:entrypoint"
cz-normalize = "knowledge.cli:entrypoint"
cz-ground = "grounding.cli:entrypoint"
cz-synthesize = "synthesis.cli:entrypoint"
cz-postman = "postman.cli:entrypoint"
cz-remediate = "remediation.cli:entrypoint"

[project.optional-dependencies]
dev = [
    "black>=23.0",
    "flake8>=6.0",
    "mypy>=1.0",
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
    "ruff>=0.5",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["scraper*", "knowledge*", "grounding*", "synthesis*", "postman*", "remediation*"]
exclude = ["tests*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
asyncio_mode = "auto"

[tool.black]
line-length = 100
target-version = ["py310"]

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
strict_equality = true

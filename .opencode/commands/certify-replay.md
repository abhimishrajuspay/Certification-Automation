# /certify replay

Folder ladder: run `replay_local.py` per folder in CSV order, capture
`out/<runId>/replay/<folder>.jsonl`, and examine failures via
`certify diagnose` before any follow-up.

Mode: OpenCode (`.opencode/agents/certify.md` contract).
